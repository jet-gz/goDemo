syntax = "proto3"; // 版本声明，使用Protocol Buffers v3版本


option go_package ="./pb"; //包名 生成的go代码也会放在里面

message Student {
    string name = 1;
    bool male = 2;
    repeated int32 scores = 3;
    map<string, int32> subject = 4;
  }